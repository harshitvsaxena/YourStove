var React = require('react');
var PropTypes = React.PropTypes;
var ReactRouter = require('react-router');
var Link = ReactRouter.Link;

function AdminList(props) {
    return (
        <div>
            Hello from Admin List UI
        </div>
    );
}

module.exports = AdminList;
