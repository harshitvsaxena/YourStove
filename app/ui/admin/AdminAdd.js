var React = require('react');
var PropTypes = React.PropTypes;
var ReactRouter = require('react-router');
var Link = ReactRouter.Link;

function AdminAdd(props) {
    return (
        <div>
            Hello From Admin Add UI
        </div>
    );
}

module.exports = AdminAdd;
